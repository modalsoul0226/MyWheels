# 🛞 Software Foundations

## Dependencies

Make sure that the following dependencies are installed:

- `Coq`
- `OCaml` and `coq-lsp` for language server support in nvim

```bash
opam install coq-lsp
```
